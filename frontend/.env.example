# Copier ce fichier en ".env" et adapter si besoin.

# URL du backend DATA LINK (FastAPI). Par défaut, le backend tourne en local
# sur le port 8000 (voir backend/README.md).
VITE_API_URL=http://localhost:8000
